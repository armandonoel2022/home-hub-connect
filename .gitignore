# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Backend runtime data (JSON storage + uploaded files).
# Data is persisted OUTSIDE the project (default C:\intranet-data on Windows),
# but if a deployment falls back to ./backend/data, never overwrite it from git.
backend/data/
backend/.env
